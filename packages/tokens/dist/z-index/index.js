/**
 * ----------------------------------------------------------
 * QUEVY UI
 * ----------------------------------------------------------
 * Z-index tokens.
 *
 * Defines the layering order used by Quevy UI components.
 * Values are intentionally separated to provide room for
 * component-specific layering without introducing arbitrary
 * z-index values.
 * ----------------------------------------------------------
 */
export const zIndex = {
    base: 0,
    dropdown: 1000,
    sticky: 1100,
    overlay: 1200,
    modal: 1300,
    popover: 1400,
    toast: 1500,
    tooltip: 1600,
};
//# sourceMappingURL=index.js.map