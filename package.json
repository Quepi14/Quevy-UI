{
  "name": "quevy-ui",
  "private": true,
  "dependencies": {
    "lit": "^3.3.3"
  },
  "scripts": {
    "dev:button": "vite --config packages/components/dev/vite.config.ts",
    "build": "pnpm --filter @quevy/core exec tsc -p tsconfig.json && pnpm --filter @quevy/tokens exec tsc -p tsconfig.json && pnpm --filter @quevy/state exec tsc -p tsconfig.json && pnpm --filter @quevy/components exec tsc -p tsconfig.json && pnpm --filter @quevy/react exec tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "@types/node": "^20.11.0"
  }
}